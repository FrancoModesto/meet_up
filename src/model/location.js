class Location {
  constructor(id, name, address, coords, image) {
    this.id = id
    this.name = name
    this.address = address
    this.coords = coords
    this.image = image
  }
}

export default Location
